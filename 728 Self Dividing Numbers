class Solution {
public:
    vector<int> selfDividingNumbers(int left, int right) {
        vector<int> res;
        for(int i = left; i <= right; i++) {
            int tmp = i;
            while(tmp > 0) {
                //if(i % (tmp % 10) == 0) tmp /= 10; 
                if(tmp % 10 == 0 || i % (tmp % 10) != 0) break;
                tmp /= 10;
            }
            if(tmp == 0) {
                res.push_back(i);
            }
        }
        return res;
    }
};
