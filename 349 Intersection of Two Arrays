//To transfer elements from a C++ std::vector to an std::set, the most efficient and common method is using the set's range constructor. 
//This automatically inserts all unique elements from the vector into the set upon creation, maintaining the set's sorted and unique properties. 

//The simplest and most efficient way to convert a set to a vector is to use the std::vector range constructor, which takes the beginning and end iterators of the set as arguments. 
//The elements are copied in the sorted order defined by the set. 

class Solution {
public:
    vector<int> intersection(vector<int>& nums1, vector<int>& nums2) {
        unordered_set<int> tmp(nums1.begin(), nums1.end());
        unordered_set<int> res;
        for(int num : nums2) {
            if(tmp.find(num) != tmp.end()) {
                res.insert(num);
            }  
        }
        return vector<int>(res.begin(), res.end());
    }
};


//to understand the ':', the part of 'for(int num : nums2)' can be rewritten in this way: remember num represents values of the vector rather than the indexes of the loop, that's why
//we need nums2.at function to get the values first in each loop
//for (int num = 0; num < nums2.size(); num++) {
//            int value = nums2.at(num);
//            if (nums_set.find(value) != nums_set.end()) {
//                result_set.insert(value);
//            }
//        }
