class Solution {
public:
    bool canConstruct(string ransomNote, string magazine) {
        int tmp[26] = {0};
        for(int i = 0; i < magazine.size(); i++) {
            tmp[magazine[i]-'a']++;
        }
        for(int j = 0; j < ransomNote.size(); j++) {
            tmp[ransomNote[j]-'a']--;
            if(tmp[ransomNote[j]-'a'] < 0) return false;
        }
        return true;
    }
};
