class Solution {
public:
    int smallestRangeI(vector<int>& nums, int k) {
        sort(nums.begin(), nums.end());
        int score = nums[nums.size()-1] - nums[0];
        if(score < 2 * k) return 0;
        else return abs(score - 2 * k);
    }
};
